#include "professor.h"

CProfessor::CProfessor(CodeType code,
                       std::string name,
                       SalaryType salary)
          : m_code(code), m_name(name), m_salary(salary)
{

}